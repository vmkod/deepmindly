database:
  # Путь указывается относительно расположения этого config.yaml
  path: "data/deepmindly.db"

watcher:
  # Частота проверки активного окна, в секундах
  interval_seconds: 2

ai:
  # Модель для построения эмбеддингов заголовков
  model_name: "all-MiniLM-L6-v2"